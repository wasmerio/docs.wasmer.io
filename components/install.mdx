import { Callout } from "nextra-theme-docs";

### Install Wasmer CLI
 
Install the latest version of _Wasmer CLI_ following the instructions [here](/install).

<Callout type="info" emoji="ℹ️">
  You can see all commands available with `wasmer --help`. You can also
  read [the CLI documentation online](/runtime/cli/).
</Callout>
