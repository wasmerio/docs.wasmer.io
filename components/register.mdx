import { Callout } from '@components/nextra-theme';

### Create an account

You can create an account directly via `wasmer login` or via the Wasmer website:
[wasmer.io/signup](https://wasmer.io/signup)
