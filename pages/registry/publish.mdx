# Publish a pacakge to Wasmer

Publishing a package to Wasmer is as simple as running the following command:

```bash
wasmer publish
```

This command will build your package, and publish it to the Wasmer registry.

It requires that you have a Wasmer account, and that you are logged in.

```bash
wasmer login
```
