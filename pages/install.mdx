# Install Wasmer

import { Callout } from "nextra-theme-docs";
import { Tabs, Tab } from "nextra-theme-docs";

Wasmer allows you to run applications either **Standalone** (_via the CLI_) or **Embedded** within [other languages](./#wasmer-language-integrations) such as C/C++, Rust, Python, Go, PHP, Ruby...

<Tabs items={['CLI (Bash)', 'CLI (PowerShell)' /*, 'JS', 'Python' */]}>
  <Tab>
    **Unix** (macOS, Linux):

    ```bash
    curl https://get.wasmer.io -sSfL | sh
    ```

    Or, to install a specific version:

    ```bash /4\.0\.0/
    curl https://get.wasmer.io -sSfL | sh -s "v4.0.0"
    ```

  </Tab>

  <Tab>
    **PowerShell** (Windows):

    ```bash
    iwr https://win.wasmer.io -useb | iex
    ```

    Or, to install a specific version:

    ```bash /4\.0\.0/
    $v="4.0.0"; iwr https://win.wasmer.io -useb | iex
    ```

  </Tab>

{/* <SyncedTabs.Tab> `bash npm install wasmer` </SyncedTabs.Tab> <SyncedTabs.Tab> ```bash pip install wasmer ``` </SyncedTabs.Tab> */}

</Tabs>

<Callout type="info">
  Other installation methods available here:
  https://github.com/wasmerio/wasmer-install
</Callout>
