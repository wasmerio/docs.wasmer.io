### Log in into Wasmer

Create a new account in [Wasmer](https://wasmer.io/sign-up).

Then, generate a new token in [your Access Tokens settings page](https://wasmer.io/settings/access-tokens). Once you have a token:

```bash
wasmer login TOKEN
```
