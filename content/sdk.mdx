import { Cards, Card } from '@components/nextra-theme'
import { RunIcon } from '@components/icons/ProductIcons'

# Introduction to Wasmer SDKs

Wasmer SDKs embed the Runtime directly in a host application. They share the
same package, sandbox, command, filesystem, and process model.

<Cards>
  <Card icon={<RunIcon width={20} height={20} />} title="JavaScript SDK" href="/runtime/javascript-sdk" />
  <Card icon={<RunIcon width={20} height={20} />} title="Python SDK" href="/runtime/python-sdk" />
</Cards>

Start with [Run your first package](/runtime/get-started) to compare the CLI,
JavaScript, and Python side by side.
