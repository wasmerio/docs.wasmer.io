import { Steps } from 'nextra-theme-docs';
import { Callout } from "nextra-theme-docs";
import { Tab, Tabs } from 'nextra-theme-docs';
import Install from '../../components/install.mdx';
import Login from '../../components/login.mdx';

# Get Started with Wasmer Edge

This page will guide you through installing Wasmer, and deploying an already existing package to Wasmer Edge infrastructure.


<Steps>
 
<Install />

<Login />

### Deploy a package

```bash
wasmer app create edge-examples/hello-world
```

This will create a `app.yaml` file with the following contents

```yaml copy filename="app.yaml"
kind: wasmer.io/App.v1
meta:
  name: <your-username>/hello-world
spec:
  workload:
    source: edge-examples/hello-world
```

So next time you can simply do `wasmer deploy` when running on a directory with an already existing `app.yaml` file.

* Do you want to serve your custom static site content? [Learn more](/edge/quickstart/static-site)

</Steps>
